import { SearchQuery } from "@comic-viewer/shared";

const searchBoxButton = document.getElementById("search-box-button");
const searshInput = document.getElementById("search-input");
searchBoxButton?.addEventListener("click",()=>{
  const currentQuery = 

  window.location.href = "";
})