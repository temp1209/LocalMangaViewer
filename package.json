{
  "name": "viewer-monorepo",
  "private": true,
  "workspaces": [
    "client",
    "server",
    "shared"
  ],
  "devDependencies": {
    "concurrently": "^9.1.2",
    "cross-env": "^7.0.3",
    "rimraf": "^5.0.5"
  },
  "scripts": {
    "clean": "rimraf client/dist server/dist",
    "build:server": "tsc --project server/tsconfig.json",
    "build:client": "cd client && vite build",
    "build": "npm run clean && concurrently \"npm:build:*\"",
    "dev:server": "cd server && cross-env NODE_ENV=development npm run dev",
    "dev:client": "cd client && cross-env NODE_ENV=development npm run dev",
    "dev": "concurrently \"npm:dev:*\"",
    "start": "cross-env NODE_ENV=production cd server && npm start",
    "start:dev": "cross-env NODE_ENV=development cd server && npm run dev",
    "preview": "npm run build && npm start"
  }
}
